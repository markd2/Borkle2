// Colors.swift - our local palette

import AppKit

enum Colors {
    static let bubbleListCellBackground_Even = NSColor.white
    static let bubbleListCellBackground_Odd = NSColor(deviceWhite: 0.970, alpha: 1.0)

    static let canvasBackground = NSColor(red: 228.0 / 255.0,
                                          green: 232.0 / 255.0,
                                          blue: 226.0 / 255.0, alpha: 1.0)
    static let bubbleBackground = NSColor.white
    static let bubbleFrame = NSColor.black
    static let bubbleConnection = NSColor.purple
}
